#include "PascalsTriangle-118.h"

int main(void) {
  Solution s;
  dumpVector2D(s.generate(1));
  dumpVector2D(s.generate(2));
  dumpVector2D(s.generate(3));
  dumpVector2D(s.generate(4));
  dumpVector2D(s.generate(5));
  return 0;
}
