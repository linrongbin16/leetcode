#include "LeetCode.h"

class Solution {
public:
  vector<vector<int>> generate(int numRows) {
    vector<vector<int>> result;
    for (int i = 1; i <= numRows; i++) {
      for (int j = 1; j <= i; j++) {
      }
    }
    return {};
  }
};

