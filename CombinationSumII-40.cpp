#include "CombinationSumII-40.h"

int main(void) {
  Solution s;

  // vector<int> v1 = {10, 1, 2, 7, 6, 1, 5};
  // dumpVector2D(s.combinationSum2(v1, 8));

  // vector<int> v2 = {2, 5, 2, 1, 2};
  // dumpVector2D(s.combinationSum2(v2, 5));

  // vector<int> v3 = {1, 2};
  // dumpVector2D(s.combinationSum2(v3, 4));

  vector<int> v4 = {3, 1, 3, 5, 1, 1};
  dumpVector2D(s.combinationSum2(v4, 8));

  return 0;
}
