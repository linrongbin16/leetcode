#include "PascalsTriangleII-119.h"

int main(void) {
  Solution s;
  dumpVector(s.getRow(1));
  dumpVector(s.getRow(2));
  dumpVector(s.getRow(3));
  dumpVector(s.getRow(4));
  dumpVector(s.getRow(5));
  return 0;
}
