#include "SpiralMatrixII-59.h"

int main(void) {
  Solution s;
  dumpVector2D(s.generateMatrix(1));
  dumpVector2D(s.generateMatrix(4));
  dumpVector2D(s.generateMatrix(5));
  dumpVector2D(s.generateMatrix(11));
  return 0;
}
